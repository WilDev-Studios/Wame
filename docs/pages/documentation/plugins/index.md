# What is a Plugin?

- A plugin is a piece of software that `wame` automatically hot-loads into an `Engine` instance.
- This allows developers to share snippets of code, preventing duplicate software and saving development time.

Visit the side navigation menu to your left to get started on `using` or `developing` plugins.
